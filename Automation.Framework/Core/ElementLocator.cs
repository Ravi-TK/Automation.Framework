namespace Automation.Framework.Core
{
    public enum ElementLocator
    {
        ID,
        Name,
        CssSelector,
        LinkText,
        PartialLinkText,
        Xpath,
        ClassName,
        TagName
    }
}